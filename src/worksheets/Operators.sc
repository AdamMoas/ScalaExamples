object Operators {

  var x = 3
  var y = 9

  var sum = x + y

  var bitwiseAND = x & y // bitwise AND
  var bitwiseOR = x | y // bitwise OR
  var bitwiseEXOR = x ^ y // bitwise exclusive OR

  var bitwiseLeftShift = x << 2
  var bitwiseRightShift = x >> 2

  val numberList = 1::2::3::4::Nil

  if (x > 0 || y > 0) "Both values are positive"

}